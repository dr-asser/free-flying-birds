/** Obstacle generation mode, matching the Java `ObstacleSeries.type` integer values. */
export enum ObstacleType {
  Triangle = 0,
  Rectangle = 1,
  Mixed = 2,
}

/** Selectable follower formation layout. */
export enum FormationType {
  Quad = 0,
  Vee = 1,
  Line = 2,
}
